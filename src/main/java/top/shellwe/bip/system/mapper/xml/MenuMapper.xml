<?xml version="1.0" encoding="UTF-8"?>

<!--
  ~ Copyright from TernaryProject (c) 2022.
  ~ Author BenjaminThomasShellwe
  ~ Date 2022/3/28 8:56:24
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.shellwe.bip.system.mapper.MenuMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="top.shellwe.bip.system.entity.SystemUrl">
		<id column="id" property="id" />
		<result column="parent_id" property="parentId" />
		<result column="url" property="url" />
		<result column="resources" property="resources" />
		<result column="title" property="title" />
		<result column="level" property="level" />
		<result column="sort_no" property="sortNo" />
		<result column="icon" property="icon" />
		<result column="type" property="type" />
		<result column="remarks" property="remarks" />
		<result column="gmt_create" property="gmtCreate" />
		<result column="gmt_modified" property="gmtModified" />
	</resultMap>


    <select id="selectMenus" resultMap="BaseResultMap">
        SELECT
             *
        FROM t_sys_menu
        WHERE
             1 = 1
        <if test="filter.id!=null and filter.id!=''">
            AND	id= #{filter.id}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findByResources" resultType="top.shellwe.bip.system.entity.SystemUrl">
		SELECT
             *
        FROM t_sys_menu
		WHERE
		1 = 1
		<if test="resources!=null and resources!=''">
			AND	resources = #{resources}
		</if>
	</select>

    <select id="selectMenuByRoleId" resultType="top.shellwe.bip.system.entity.SystemUrl">
		SELECT sm.* FROM t_sys_menu sm
	    LEFT JOIN t_sys_role_menu srm ON sm.id = srm.menu_id
	    WHERE srm.role_id = #{roleId}
	</select>

</mapper>
